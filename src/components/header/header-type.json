{
  "collectionName": "components_header_header_types",
  "info": {
    "displayName": "Header Type",
    "icon": "photo-video",
    "description": ""
  },
  "options": {},
  "attributes": {
    "background_transparency": {
      "type": "enumeration",
      "enum": [
        "opaque",
        "transparent"
      ],
      "required": true,
      "default": "opaque"
    },
    "logo": {
      "type": "enumeration",
      "enum": [
        "logo",
        "alt_logo",
        "inverted_logo"
      ],
      "default": "alt_logo"
    },
    "use_navigation": {
      "type": "boolean",
      "default": true,
      "required": true
    }
  }
}
