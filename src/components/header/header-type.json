{
  "collectionName": "components_header_header_types",
  "info": {
    "displayName": "Header Type",
    "icon": "photo-video",
    "description": ""
  },
  "options": {},
  "attributes": {
    "background_transparency": {
      "type": "enumeration",
      "enum": [
        "opaque",
        "transparent"
      ],
      "required": true,
      "default": "opaque"
    },
    "logo": {
      "type": "enumeration",
      "enum": [
        "logo",
        "alt_logo"
      ],
      "default": "alt_logo",
      "required": true
    },
    "use_navigation": {
      "type": "boolean",
      "default": true,
      "required": true
    },
    "use_special_layout": {
      "type": "boolean",
      "default": false,
      "required": true
    }
  }
}
